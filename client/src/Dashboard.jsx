import React from 'react';

const Dashboard = () => {
  return (
    <div style={{ padding: '2rem', color: 'white' }}>
      <h1 style={{ fontSize: '2rem', marginBottom: '1rem' }}>Executive Dashboard</h1>
      <div style={{ 
        padding: '3rem', 
        border: '2px dashed #334155', 
        borderRadius: '1rem', 
        textAlign: 'center',
        color: '#94a3b8' 
      }}>
        <p>🚀 Waiting for friend's code to integrate...</p>
        <p style={{ fontSize: '0.8rem', marginTop: '1rem' }}>
          This is a placeholder so the app doesn't crash.
        </p>
      </div>
    </div>
  );
};

export default Dashboard;